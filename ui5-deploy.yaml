# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: it.orogel.zsalesagentcommissions
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: S4HANA
          url: http://orogeldev:44333
        app:
          name: ZSALESAGENTCOMM
          description: CBO Sales Agent Commissions
          package: Z001
          transport: SODK901061
